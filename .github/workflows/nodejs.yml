name: Node CI

on: [push]

jobs:
  helle_world_job:
    runs-on: self-hosted
    name: A job to say hello
    steps:
    - name: command-action
      uses: github/command@v1.1.1
      with:
        command: echo $HOME
