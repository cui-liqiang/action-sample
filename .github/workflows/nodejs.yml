name: Node CI

on: [push]

jobs:
  helle_world_job:
    runs-on: self-hosted
    name: A job to say hello
    steps:
    - name: command-action
      uses: actions/github-script@v2.1.0
      with:
        command: echo $HOME
